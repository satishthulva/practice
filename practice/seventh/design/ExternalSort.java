/**
 * <pre>
 * Given that you have 10GB file to be sorted and the machine RAM is only 1 GB, how do you do it ?
 * Assume one line for each record in the file. The record format is timestamp [ TAB ] data. Data is a string of arbitrary length.
 * </pre>
 * @author rebecca
 */
public class ExternalSort {
	// External merge sort
	// Records are not of equal length. Be aware of that
}
